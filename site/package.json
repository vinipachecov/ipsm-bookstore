{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://vinipachecov.github.io/ipbsm-bookstore/",
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "xls-to-json": "^0.5.0"
  },
  "scripts": {
    "start": "HOST=192.168.0.14 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
